# S-SERVO — ROUND 2 adversarial review (independent re-audit)

Slice: S-SERVO (shared PURE visual-servo math). Module: finals/mission/phases/_servo.py. Test: finals/tests/test_servo.py.
Baseline re-run: 66 passed (bare venv, 0.21s) against the UNCHANGED real files (git status clean for both). All mutants run in a deleted scratch sandbox (real source/tests never edited).
Method: each claimed mutant text-substituted on a SOURCE-STRING copy, loaded standalone, exercised against the REAL test_servo.py assertions (incl. parametrize expansion); SURVIVED/KILLED reported with failing-assert counts.

## VERDICT per round-1 finding
test_servo.py:203 (MED TEST: k never non-unit -> drop-k survives): CONFIRMED. Scratch-proven: dropping `k *` (and `k*`->`/k`) leaves all 66 cases green; numeric probe at px+160/alt2: baseline step scales 50/100/25 at k=1/2/0.5 but drop-k pins 50 for every k. Live coverage gap.
module_map.md:123 (MED TEST: "wrap180 >= was equivalent dead-code" note is WRONG): CONFIRMED. `>=` maps wrapped==180.0 -> -180.0 at every multiple-of-180 input (180/-180/540/-540 all DIFFER from `>`); the `>` is load-bearing for the closed +180 end. The `>=` mutant is KILLED (5 asserts: wrap180_values 180/-180/540/-540 + the interval sweep), so the note is harmless to safety but factually false and invites a future `>`->`>=` "simplify".
_servo.py:135/194/133/190/82 (round-1 sign/deadband/wrap KILLED claims): CONFIRMED all. rotate-sign flip KILLED (7 asserts); LEFT<->RIGHT swap KILLED (3); rotate deadband <=->< KILLED (1, test:116); pixel deadband <=->< KILLED (2, test:214+:298); wrap180 >->>= KILLED (5).
test_servo.py:185 (LOW BUG: bbox[1]/[3] y NaN/inf unchecked): CONFIRMED. Off-centre bbox (460,nan,500,inf) emits Move(distance_cm=50), no raise — y is never read so harmless to THIS fn; matches perception.py bearing_from_bbox which also only checks idx 0/2 (consistent by-design). LOW stands.
test_servo.py:262 (LOW TEST: *100.0 and /frame_w pinned only transitively): CONFIRMED-as-noted. cm-const 100->200 KILLED (2) and /frame_w->/(2*frame_w) KILLED (2) — both die via the doubling test; round-1 correctly flags fragility, not a live gap. DUPLICATE-of-intent with the suggested explicit-cm assertion.
_servo.py:194 (LOW TEST: px>0 vs px>=0 at px==0 is dead/equivalent): CONFIRMED. px==0,tol_px==0 -> None (deadband catches it first); px>=0 mutant SURVIVED all 66 — equivalent, NOT a gap.
_servo.py:135 (LOW OPT: clamp re-validates error/bounds per tick; REJECT inlining min/max): CONFIRMED. bearing_error_to_rotate is in navigate.step() (navigate.py:293) = hot. Re-REJECT any min/max inline (drops the lo>hi fail-loud guard). SCALING note only.
_servo.py:193 (LOW OPT: clamp re-validates min_cm/max_cm per tick; "no per-call alloc exists"): CONFIRMED on the clamp re-validation; the "no per-call allocation" half is REFUTED — see new finding below (f-string alloc).
_servo.py:195 (LOW BUG: int(round()) banker's rounding -> 0 cm Move only if min_cm==0): CONFIRMED + RESOLVED. Scratch: min_cm=0 + tiny/0.5-raw step -> Move(distance_cm=0). In the shipped path land_on_pad min_step_cm defaults 5 / validated `_pos_int` >=1 and landing_real.json never overrides it, so min_cm>=1 always -> 0 cm Move UNREACHABLE in production. LOW stands.

## NEW findings (round 1 missed)
finals/mission/phases/_servo.py:186: LOW OPT: `f"bbox_xyxy[{i}]"` is FORMATTED on every call (twice/tick), BEFORE the deadband early-return at line 190 — a real per-tick str allocation on the hot path (land_on_pad.step() pixel_offset_to_move). REFUTES round-1:193's "No per-call list/dict/tuple allocation exists". Hoist the two diagnostic names to a module constant `_BBOX_NAMES = ("bbox_xyxy[0]", "bbox_xyxy[2]")` and zip; does NOT touch sign logic. Negligible impact (arena tiny) — SCALING note.
finals/mission/phases/_servo.py:186: LOW BUG: a too-short bbox tuple (len < 3) raises bare `IndexError` at `bbox_xyxy[i]`, NOT the typed ValueError WHAT/WHICH/WHY/CHECK contract every other bad input here gets — a detector emitting a malformed bbox crashes the servo with an opaque trace. Perception.py:bearing_from_bbox has the SAME latent gap (indexes [0]/[2] without a length guard). Add a length/shape check that raises the typed ValueError naming Sighting.bbox_xyxy. No test pins this (test_servo.py only feeds 4-tuples) — a mutant that drops a length guard could never be killed because the guard does not exist.
finals/mission/phases/_servo.py:135: LOW TEST: NO test pins the EXACT magnitude of a NON-clamped, NON-deadband rotate at a sub-max error (only sign + the +/-3 cases at max_step=30 and the +/-30 clamp). A mutant scaling the rotate by a constant k_rot (e.g. `Rotate(0.5*clamp(...))`) IS killed by the ==Rotate(3.0)/==Rotate(-20.0) exact-value asserts — verified the err-swap (yaw-target) KILLED 8 and wrap180-negate KILLED 20 — so this is covered; recording so round 3 does not re-flag. (No gap; consistency note.)
finals/mission/phases/_servo.py:194: INFO: the px>0/px<0 -> RIGHT/LEFT split is verified consistent with the module docstring's body-frame "chase-the-blob" convention AND deliberately OPPOSITE perception.py's right-pixel->CW-bearing world-frame sign; both correct in their own frame, both test-pinned. No NEW sign-swap mutant survives: rotate-sign, subtraction-order (target-yaw vs yaw-target, KILLED 8), wrap180-negate (KILLED 20), LEFT<->RIGHT, and the rotate clamp-bound-swap (KILLED 8 via the lo>hi guard) all die. The sign-critical surface is fully covered.
finals/mission/phases/_servo.py:67: INFO: wrap180 interval invariant (-180,180] verified over 200k random uniforms in [-1e6,1e6] plus 1e16/-1e16/180.0000001/359.9999999 edges — 0 out-of-interval, no -180 leak, no NaN/inf escape. Robust.

## Severity tally (NEW only)
CRIT 0, HIGH 0, MED 0, LOW 4 (2 OPT, 1 BUG untyped-IndexError, 1 TEST-as-INFO consistency), INFO 2.

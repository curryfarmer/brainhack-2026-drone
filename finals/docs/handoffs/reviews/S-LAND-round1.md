# S-LAND round 1 — adversarial review (phases/land_on_pad.py + test_land_on_pad.py)

Scope: ACQUIRE→CENTER→DESCEND→COMMIT machine, never-battery-dead-hover guarantee,
loss-recovery bounds, N-of-M acquire, fail-loud bar, [OPT] step() hot path. 63 tests green at review time.

## BUGS / correctness
finals/mission/phases/land_on_pad.py:540: MED BUG: confirm-before-descend gate `self._recent_hits() >= self.descend_persist_frames` is correct, but `_recent` includes THIS frame's hit (appended at step() line 418) AND every prior CENTER/scan tick's hit, so the "frames the pad was confirmed in" actually counts centering-hold frames too — the gate can be satisfied by center-hold persistence rather than descend-specific confirmation. Not a hover risk (bounded by budget) but the documented "confirm-before-descend" semantic is weaker than the docstring claims. Document that the window is shared center+descend confirmation, or keep a descend-local hit counter reset on entry to PAD_DESCEND.
finals/mission/phases/land_on_pad.py:380: LOW BUG: `is_flying is False` and the Fallback short-circuit (line 391→Land) mean that if telemetry reports `is_flying is None` AFTER a Fallback Land was commanded, the phase re-emits Land() forever and NEVER reaches Done — it cannot battery-dead-hover (it keeps commanding Land, which grounds the airframe), but the phase will not terminate to Done while is_flying stays None. Acceptable for the no-hover guarantee; note that phase completion depends on the adapter eventually reporting is_flying False after a Land.
finals/mission/phases/land_on_pad.py:535: LOW BUG: re-gating from PAD_DESCEND back to PAD_CENTER on drift resets `_center_streak` but never resets `_loss_retries`; loss retries accumulate across the whole phase even after long stretches of good progress, so a flickery-but-recovering approach can hit Fallback on cumulative (not consecutive) losses. Conservative-safe (still Lands), but tune max_loss_retries with this in mind or reset on a clean centred descend step.

## MISSING / WEAK tests
finals/tests/test_land_on_pad.py:341: MED TEST: commit boundary is untested at EXACTLY commit_alt_m — `test_commit_below_alt_lands` uses 0.4 (<0.5) and `test_commit_not_triggered_above_floor` uses 1.0 (>0.5). Mutant: flip `alt_m <= self.commit_alt_m` to `<` at land_on_pad.py:412 — SURVIVES the suite. Kill it: a test with `altitude_m == commit_alt_m` asserting Land.
finals/tests/test_land_on_pad.py:212: MED TEST: the descend confirm gate is untested at EXACTLY descend_persist_frames — `test_descend_only_when_centered_and_persistently_seen` runs with a window FULL of hits (recent_hits >> persist) and the holds test has recent_hits < persist. Mutant: flip `>= self.descend_persist_frames` to `>` at land_on_pad.py:540 — SURVIVES (no test where recent_hits == descend_persist_frames expects a DOWN). Kill it: enter PAD_DESCEND with recent_hits set to exactly descend_persist_frames and assert Move(DOWN).
finals/tests/test_land_on_pad.py:284: MED TEST: budget-exceeded is tested only in PAD_ACQUIRE and PAD_CENTER, never in PAD_DESCEND. The budget gate is centralized (land_on_pad.py:397) so a blanket-removal mutant is caught, but there is no regression guard that a future per-substate refactor still bounds DESCEND. Kill-proof it: a test that forces PAD_DESCEND then steps with phase_elapsed >= total_budget_s and asserts Fallback Land.
finals/tests/test_land_on_pad.py:103: MED TEST: a NON-valid (decoy/red-pad) marker is proven to never ACQUIRE, but there is no test that a decoy marker present WHILE a valid pad is also in frame never becomes the centering target, and no test that a decoy alone in PAD_CENTER/PAD_DESCEND counts as "lost" (drops to acquire / triggers loss-retry). Mutant: drop the `marker_id in self.valid_marker_ids` filter in `_valid_sightings` (line 319-321) and accept any id — the existing acquire-ignores test would catch acquire, but the center/descend "decoy == lost" behaviour is unguarded. Kill it: in PAD_CENTER feed ONLY a decoy id and assert it routes to PAD_ACQUIRE (treated as lost), not centered.
finals/tests/test_land_on_pad.py:368: LOW TEST: `_pick_target` largest-bbox / lowest-id tie-break is tested, but determinism across ticks (the docstring's "no flapping target choice") is not pinned — feed the same two-pad frame twice and assert the same target id both ticks (kills a mutant that reverses the bbox-area key or the `-marker_id` tie-break sign, e.g. `max`→`min`).
finals/tests/test_land_on_pad.py:428: LOW TEST: the defensive unreachable-substate RuntimeError (land_on_pad.py:428-430) and the FALLBACK-state non-terminal short-circuit path are not directly asserted; FALLBACK is reached only via fallback transitions. Low value (defensive code), noted for completeness.

## CODE-OPTIMIZATION PASS
finals/mission/phases/land_on_pad.py:332: LOW OPT [hot path]: `_pick_target` rebuilds a closure lambda `key=lambda s: (self._bbox_area(s), -s.marker_id)` every call, and it is called up to twice per tick (CENTER and on the CENTER→DESCEND re-gate). Negligible at ~10 Hz with ≤ a handful of sightings; flag only as a micro-alloc note — do NOT change if it costs readability. Est. impact ~nil.
finals/mission/phases/land_on_pad.py:319: LOW OPT [hot path]: `_valid_sightings` builds a fresh list every tick — unavoidable (it is the per-tick filter result the substates consume) and tiny (sightings/tick is small). SCALING note only; no action.
finals/mission/phases/land_on_pad.py:104: INFO OPT: servo math is correctly delegated to the shared `_servo.pixel_offset_to_move` (no duplicated lateral/altitude math) — the DRY/no-duplicate-math requirement is satisfied. No fail-loud check or budget gate is on a removable cold path that an opt could weaken. No OPT here would be accepted that touches the line-397 budget gate or the altitude-finite guards.

## CONVENTIONS (mechanical)
finals/mission/phases/land_on_pad.py: PASS: no bare `except` / no `except Exception`; no top-level numpy or SDK import (stdlib enum/math/collections/typing only); no while-loops (so no unbounded-loop class); errors are typed (ConfigError in __init__, Abort/Done Actions in step); units in names (_cm/_m/_deg/_s); fail-loud messages carry WHAT/WHICH(drone_id)/WHY/CHECK (Abort at 350-357 and 364-368, Fallback reasons at 398-404 / 449-456 / 511-516). frame_w = frame_shape[1] is the CORRECT width index (types.py frame_shape=(h,w); perception.py uses [1] identically) — NO axis-swap bug. N-of-M acquire uses `>=` at exactly-N (line 440), pinned by test_acquire_on_3_of_5; the `>`-mutant on acquire IS killed.

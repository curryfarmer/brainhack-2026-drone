# S-PLAN review — ROUND 2 (independent re-audit)

Slice: planning/types.py, planning/polygon_tools.py, planning/visibility_graph.py, errors.py::PlanningError
Tests: test_arena_skeleton.py, test_polygon_tools.py, test_visibility_graph.py
Baseline re-confirmed: 64/64 green on the bare venv (numpy-less).
Method: every TEST-gap mutant was APPLIED to a scratch copy, the real suite re-run, then byte-restored (git clean verified). No source/test file changed.

One finding per line: `path:line: SEV tag: problem. fix.`

## VERDICTS on round-1 findings

VERDICT visibility_graph.py:227 (LOW BUG subdivision/inflation link): CONFIRMED. max_leg_cm is caller-supplied and has no derivation from inflation_m; the docstring (lines 7-8, 35-36) claims subdivision "bounds per-move open-loop drift under the inflation margin" which is unbacked. The fix is a docstring downgrade or a NAV-5 budget link — not a code bug in this slice. Severity LOW correct.
VERDICT polygon_tools.py:120 (LOW BUG reflex-vertex miter direction): CONFIRMED. abs(sin_half) + sum-of-normals mis-aims the miter at a reflex vertex; safe only because crate footprints are convex and NAV-2 does NOT enforce convexity/simplicity. See NEW finding at polygon_tools.py:102 for the sharper, separately-reachable sub-case round 1 did not isolate.
VERDICT polygon_tools.py:241 (LOW BUG midpoint-only interior test on concave): CONFIRMED but UNREACHABLE for convex inflated crates (verified: a chord between two boundary points of a convex polygon has its midpoint interior; diagonal corner-to-corner of an inflated square returns True). Severity LOW correct; no code change for convex inputs.
VERDICT visibility_graph.py:220 (LOW CONV _reconstruct while unbounded comment): CONFIRMED. Provably bounded (acyclic came_from tree), but lacks the parity bound comment every other while carries (convention 3). Documentation-only.
VERDICT visibility_graph.py:183 (HIGH TEST A* optimality unpinned): CONFIRMED BY EXPERIMENT. Applied `h*1.5` (inadmissible) to the real module: 28/28 visibility tests still PASS. The optimal south-detour on the proposed asymmetric box ((1,-1),(1,3),(3,3),(3,-1)) inflated 0.3, start (0,0) goal (4,0) = 555.3 cm; no existing test asserts the cost is minimal, only that the goal is reached collision-free. The proposed kill test (assert total ≈ 555 cm and strictly < the ~900 cm north route) is correct and necessary.
VERDICT visibility_graph.py:198 (MED TEST goal-on-pop vs goal-on-relaxation): CONFIRMED BY EXPERIMENT. Applied the mutant (remove the pop-time `if current==goal_i: return`, return immediately after `came_from[nxt]=current`): 28/28 PASS. The asymmetric-detour optimality test above also kills this. Note: with the CONSISTENT euclidean heuristic the first relaxation of the goal is already optimal, so this mutant is *behaviourally equivalent* on a consistent h — it only diverges if h becomes inconsistent (i.e. it is co-killed by, and only by, the optimality test). Round 1's "it doubles as the kill" is accurate.
VERDICT visibility_graph.py:146 (MED TEST buried-corner drop untested): CONFIRMED BY EXPERIMENT, with a correction. Removing the drop entirely (KEEP all corners) → 28/28 PASS (the real gap). NOTE: round 1's *alternative* mutant `any(...)→not any(...)` is actually KILLED (4 failed — it drops the USEFUL corners and breaks basic detours), so only the "keep-all" / "always-False predicate" form survives. The proposed kill (two overlapping inflated keep-outs, assert the interior corner is absent from the node set or unrouted) is the right one; word it as keep-all, not as the `not any` flip.
VERDICT polygon_tools.py:177 (LOW TEST collinear-overlap branch partial): CONFIRMED BY EXPERIMENT. Dropping ONLY the o1/o2 collinear sub-branches (a polygon vertex lying on the interior of segment a-b, the collinear-overlap-along-an-edge case) → 26/26 PASS; the two existing tests cover only o3/o4 (a segment endpoint touching an edge). Round 1's proposed (0.5,0)→(2,0)-along-south-edge assertion is needed. (Dropping ALL four branches IS caught — 2 failed — so the gap is narrow but real.)
VERDICT types.py:121 (LOW TEST >=3-distinct rule not in this slice): CONFIRMED as DEFER. The rule lives in KeepOut.from_dict and is exercised by the sibling S-FRAME test_arena_config.py per the docstring; this slice's test_arena_skeleton.py does not assert it. Correctly deferred — do not duplicate here.
VERDICT visibility_graph.py:176 (LOW OPT symmetric-visibility memo): CONFIRMED. Cold path, V≤~20, sub-ms; scaling note only, do NOT apply (would add state). Accurate.
VERDICT visibility_graph.py:117 (LOW OPT no redundant re-inflation / real heap): CONFIRMED. Inflation runs once per keep-out in the comprehension; A* uses a real heapq (lines 190,197,213), not a linear min-scan. Clean, no action.
VERDICT visibility_graph.py:183 (LOW OPT precompute h per node): CONFIRMED + REJECT stands. One hypot per push over ≤~20 nodes; pure perf on the cheapest path; reject for readability. Accurate.

Round-1 mechanical-scan claims spot-checked and CONFIRMED: no bare/except-Exception in the three modules; no top-level numpy/SDK (stdlib math/heapq/typing + finals.errors/types only); both while loops bounded; PlanningError sites (125,131,153) carry WHAT/WHICH/WHY/CHECK and ValueError guards (102-115) are typed+actionable; determinism holds across PYTHONHASHSEED 0/1/2/42 on a symmetric equal-cost tie (node list insertion order + integer tie-break counter → no dict-order leak); the >/>= straddle equivalent-mutant claim is sound (on-edge guard intercepts every distinguishing point).

## NEW findings round 1 MISSED

finals/mission/planning/polygon_tools.py:102: MED BUG: `s = 1.0 if _signed_area(pts) > 0.0 else -1.0` picks INWARD normals whenever the signed area is EXACTLY 0.0 — i.e. a self-intersecting/symmetric ring (a "bowtie" like [[0,0],[2,2],[0,2],[2,0]]). Verified: that bowtie has shoelace area 0.0 and inflate_polygon offsets the corners the WRONG way (toward the interior), SHRINKING the keep-out and silently re-admitting a collision. This is REACHABLE from config: NAV-2 (types.py KeepOut.from_dict) accepts the bowtie (it only checks ≥3 DISTINCT vertices, never simple-polygon/non-self-intersecting). Distinct from round-1's reflex-vertex note (that was about concave INPUT producing self-intersecting OUTPUT; this is a degenerate-area INPUT flipping the global sign). Fix: reject self-intersecting / zero-signed-area rings at NAV-2 (or raise in inflate_polygon when `abs(_signed_area) < eps`), since a zero-area "obstacle" has no defined outward direction; do NOT silently default to -1.
finals/mission/planning/polygon_tools.py:102: LOW TEST: no test pins the signed-area sign-selection on a near-degenerate ring. A mutant flipping the comparison to `>= 0.0` (or the tie-branch) is uncaught by the slice. Kill it together with the bowtie/zero-area case above: assert inflate of a self-intersecting/zero-area ring is REFUSED (after the fix) rather than silently shrinking.
finals/mission/planning/visibility_graph.py:209: LOW TEST: the relaxation guard `tentative < g[nxt]` weakened to `tentative <= g[nxt]` SURVIVES the suite (verified: 28/28 PASS). It does not change path COST (so the optimality test won't catch it) — it only re-points predecessors on exact-cost ties, which CAN perturb the chosen route between two equal-cost detours and break the determinism guarantee under a different node ordering. Round 1 covered optimality and determinism separately but did not name this specific `<`→`<=` survivor. Kill it: assert plan() on a symmetric equal-cost arena returns the SPECIFIC expected leg list (pin the tie-resolution), not just "deterministic across two identical calls" (test_plan_is_deterministic compares two calls in ONE process and cannot see a tie-resolution change).
finals/mission/planning/visibility_graph.py:91: LOW CONV: plan() validates start/goal are finite and not inside a keep-out, but never checks they lie within arena.bounds_m — a goal far outside the arena plans a clean straight shot (verified: goal (999,999) in a 10×10 arena yields 15 legs aimed at the wall). This is the DOCUMENTED division of labour (bounds/geofence belong to NAV-5/guards, and plan() is scoped to keep-outs), so it is informational, not a bug; flag only so the seam is explicit. No code change in this slice.

## OPT pass [OPT] (cold path — plan() runs ONCE per phase over a ≤~20-vertex arena)

finals/mission/planning/visibility_graph.py:203: LOW OPT: COLD path. The A* neighbour loop scans `range(n)` and calls visible(current,nxt) for every node each expansion → the documented ~O(V²) edge tests × O(polys·verts) each, NOT accidental O(V³). At V≤~20 this is sub-ms. Memoizing the symmetric visible(i,j) would halve the segment tests but adds state on a cold path — SCALING note only, do NOT apply (matches round-1 :176). No convention conflict.
finals/mission/planning/visibility_graph.py:146: LOW OPT: COLD path. Node construction is O(corners²) point-in-polygon (each corner tested against every OTHER inflated poly). Correct and cheap at this scale; the only redundancy is that point_in_polygon re-walks each poly's edges — not worth hoisting on a once-per-phase build. No action.
finals/mission/planning/polygon_tools.py:61: LOW OPT: COLD path. _signed_area is recomputed inside inflate_polygon on every call, but inflate runs once per keep-out at plan() time (cold). No per-tick or per-expansion recomputation. No action. (REJECT any hoist that would obscure the winding logic — correctness > clarity > perf.)

No CRIT/HIGH NEW findings. The single HIGH remains round-1's A* optimality TEST gap (CONFIRMED by experiment).

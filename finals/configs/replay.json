{
  "_comment": "Laptop-only dev: frames from disk -> ArUco (always on, the PRIMARY detector) -> sightings.csv. 0 drones, no flight. YOLO is OFF by default; to exercise it set detector backend 'ultralytics' + weights (best.pt = qualifier barrel model, plumbing only).",
  "profile": "replay",
  "flight_backend": "none",
  "frame_backend": "replay",
  "replay_dir": "finals/tests/fixtures/frames",
  "detector": { "backend": "none" },
  "drones": []
}

# Edited by Claude — added mss for screen-capture fallback. See docs/AUTHORSHIP.md.
# RoboVerse Qualifier 2026 — drone autonomy stack
# Install:  pip install -r requirements.txt
# Then install torch separately per platform (see docs/quali/deployment.md, "Install").
# Gazebo Python bindings (gz.transport13, gz.msgs10) are NOT pip-installable —
# install Gazebo Harmonic system pkg (brew install gz-harmonic | apt install gz-harmonic).

# --- Drone / autopilot ---
mavsdk>=2.0
pymavlink

# --- Vision / detection ---
ultralytics>=8.2,<9
torch>=2.1                # MPS fallback for Apple Silicon
opencv-python
Pillow
mss                       # screen-capture fallback for collect_yolo_data when Gazebo camera unavailable

# --- Numeric / planning ---
numpy<2          # torch 2.2.x wheels were built against NumPy 1.x
scipy
matplotlib
PyYAML

# --- Notebooks (Train_YOLO_Models*.ipynb local runs) ---
jupyter
ipykernel

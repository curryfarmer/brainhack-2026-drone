# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual env
.venv/
venv/

# macOS
.DS_Store

# Training artifacts (regenerable)
runs/
runs_finals/
detected_images/

# Data + models (large; share via separate channel)
data/_smoke/
data/train/
data/validation/
data/test/
data/session_meta/
data/.validated.flag
data/.split.log
data/data.yaml
data/validation_report.json
data/import.log
models/yolo_*/
models/*.pt
# keep models/latest_path.txt and model_config.json so newcomers see the schema

# Logs
*.log
logs/

# SITL launcher runtime state (PID files; logs covered by *.log above)
sim/run/

# Model weights (large; live under models/ which is already ignored)
*.pt
# Briefing source + extraction scratch (share via separate channel)
*.pptx
.pptx_extract/
# YOLO data staging
finals/yolo_parts/
